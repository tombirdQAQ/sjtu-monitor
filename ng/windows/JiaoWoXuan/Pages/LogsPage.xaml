<Page
    x:Class="JiaoWoXuan.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JiaoWoXuan"
    xmlns:core="using:JiaoWoXuan.Core">

    <Grid Padding="24,16,24,20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <SelectorBar x:Name="LevelBar" SelectionChanged="OnLevelChanged" />
            <AutoSuggestBox x:Name="QueryBox" Grid.Column="2" PlaceholderText="筛选日志" QueryIcon="Filter" TextChanged="OnQueryChanged" />
            <ToggleButton x:Name="FollowToggle" Grid.Column="3" IsChecked="True" Content="跟随最新" />
        </Grid>
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <ListView x:Name="Entries" SelectionMode="Extended" Padding="0,6">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                            <Setter Property="MinHeight" Value="26" />
                            <Setter Property="Padding" Value="12,1" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="core:LogEntry">
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="64" /><ColumnDefinition Width="40" /><ColumnDefinition Width="96" /><ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind TimeText}" FontFamily="Cascadia Mono, Consolas" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{x:Bind LevelText}" FontSize="12" FontWeight="SemiBold"
                                           Foreground="{x:Bind local:Ui.ToneForeground(LevelTone)}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{x:Bind Source}" FontSize="12" TextTrimming="CharacterEllipsis"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="3" Text="{x:Bind Message}" FontFamily="Cascadia Mono, Consolas" FontSize="12"
                                           TextWrapping="Wrap" IsTextSelectionEnabled="True" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock x:Name="Empty" Text="暂无符合条件的日志" Style="{StaticResource SecondaryText}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</Page>
