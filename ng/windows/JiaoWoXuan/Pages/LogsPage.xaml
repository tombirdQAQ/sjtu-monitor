<Page
    x:Class="JiaoWoXuan.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JiaoWoXuan"
    xmlns:pages="using:JiaoWoXuan.Pages"
    Background="Transparent">

    <Grid Padding="24,12,24,20" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="日志" FontSize="26" FontWeight="SemiBold" VerticalAlignment="Center" />
            <AutoSuggestBox x:Name="QueryBox" Grid.Column="1" PlaceholderText="筛选日志内容或来源" QueryIcon="Find"
                            CornerRadius="16" VerticalAlignment="Center" TextChanged="OnQueryChanged" />
            <ToggleButton x:Name="FollowToggle" Grid.Column="2" IsChecked="True" CornerRadius="16" Padding="12,6">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE74B;" FontSize="13" />
                    <TextBlock Text="跟随最新" />
                </StackPanel>
            </ToggleButton>
        </Grid>

        <!-- 级别统计卡片,兼作筛选 -->
        <Grid x:Name="LevelCards" Grid.Row="1" ColumnSpacing="12" />

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="6" CornerRadius="22">
            <Grid>
                <ListView x:Name="Entries" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                            <Setter Property="MinHeight" Value="34" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="2,1" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="pages:LogRowItem">
                            <Grid ColumnSpacing="12" Padding="12,6" CornerRadius="10" Background="{x:Bind RowBackground}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="64" />
                                    <ColumnDefinition Width="76" />
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Entry.TimeText}" FontFamily="Cascadia Mono, Consolas" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                <Border Grid.Column="1" CornerRadius="9" Padding="7,2" HorizontalAlignment="Left" VerticalAlignment="Center"
                                        Background="{x:Bind LevelBackground}">
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <FontIcon Glyph="{x:Bind LevelGlyph}" FontSize="11" Foreground="{x:Bind LevelForeground}" />
                                        <TextBlock Text="{x:Bind Entry.LevelText}" FontSize="12" FontWeight="SemiBold" Foreground="{x:Bind LevelForeground}" />
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" CornerRadius="9" Padding="7,2" HorizontalAlignment="Left" VerticalAlignment="Center"
                                        BorderThickness="1" BorderBrush="{ThemeResource GlassStrokeBrush}">
                                    <TextBlock Text="{x:Bind Entry.Source}" FontSize="12" TextTrimming="CharacterEllipsis"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </Border>
                                <TextBlock Grid.Column="3" Text="{x:Bind Entry.Message}" TextWrapping="Wrap" IsTextSelectionEnabled="True"
                                           Foreground="{x:Bind MessageForeground}" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel x:Name="Empty" Spacing="8" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE81C;" FontSize="32" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    <TextBlock x:Name="EmptyText" Style="{StaticResource BodyStrongTextBlockStyle}" HorizontalAlignment="Center" />
                    <TextBlock Text="监控、抓取的输出与选课变动都会出现在这里" Style="{StaticResource SecondaryText}" HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
