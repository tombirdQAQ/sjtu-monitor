<Page
    x:Class="JiaoWoXuan.Pages.SwapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JiaoWoXuan"
    xmlns:pages="using:JiaoWoXuan.Pages"
    xmlns:core="using:JiaoWoXuan.Core">

    <Grid Padding="24,16,24,20" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorder}">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="自动换课" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="监控发现更高优先级课程有空位时自动退旧选新；只升级不降级，时间冲突的课程不会被选择。设置在重启监控后生效。"
                               Style="{StaticResource SecondaryText}" TextWrapping="Wrap" />
                </StackPanel>
                <RadioButtons x:Name="ModeButtons" Grid.Column="1" MaxColumns="3" SelectionChanged="OnModeChanged" VerticalAlignment="Center">
                    <x:String>关闭</x:String>
                    <x:String>演练</x:String>
                    <x:String>真实启用</x:String>
                </RadioButtons>
            </Grid>
        </Border>

        <TextBlock Grid.Row="1" Text="方案执行状态" Style="{StaticResource BodyStrongTextBlockStyle}" />
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Padding="16,0" ColumnSpacing="10" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*" /><ColumnDefinition Width="60" /><ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="80" /><ColumnDefinition Width="60" /><ColumnDefinition Width="90" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="方案" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="1" Text="类型" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="2" Text="当前持有" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="3" Text="监控目标" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="4" Text="完成" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="5" Text="失败" Style="{StaticResource ColumnHeader}" />
                </Grid>
                <ListView x:Name="GroupRows" Grid.Row="1" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="pages:SwapGroupRow">
                            <Grid ColumnSpacing="10" Padding="0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.2*" /><ColumnDefinition Width="60" /><ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="80" /><ColumnDefinition Width="60" /><ColumnDefinition Width="90" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name}" />
                                <TextBlock Grid.Column="1" Text="{x:Bind Kind}" />
                                <TextBlock Grid.Column="2" Text="{x:Bind Held}" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="3" Text="{x:Bind Watched}" />
                                <TextBlock Grid.Column="4" Text="{x:Bind Completed}" />
                                <local:StatusBadge Grid.Column="5" Text="{x:Bind Failed}" Tone="{x:Bind FailedTone}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <TextBlock Grid.Row="3" Text="换课记录" Style="{StaticResource BodyStrongTextBlockStyle}" />
        <Border Grid.Row="4" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Padding="16,0" ColumnSpacing="10" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" /><ColumnDefinition Width="60" /><ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" /><ColumnDefinition Width="120" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="时间" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="1" Text="模式" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="2" Text="方案" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="3" Text="课程" Style="{StaticResource ColumnHeader}" />
                    <TextBlock Grid.Column="4" Text="结果" Style="{StaticResource ColumnHeader}" />
                </Grid>
                <ListView x:Name="HistoryRows" Grid.Row="1" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="core:SwapHistoryRow">
                            <Grid ColumnSpacing="10" Padding="0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160" /><ColumnDefinition Width="60" /><ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="2*" /><ColumnDefinition Width="120" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind TimestampText}" />
                                <TextBlock Grid.Column="1" Text="{x:Bind ModeText}" />
                                <TextBlock Grid.Column="2" Text="{x:Bind GroupText}" />
                                <TextBlock Grid.Column="3" Text="{x:Bind CourseText}" TextTrimming="CharacterEllipsis" />
                                <local:StatusBadge Grid.Column="4" Text="{x:Bind ResultText}" Tone="{x:Bind ResultTone}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock x:Name="NoHistory" Grid.Row="1" Text="暂无换课记录" Style="{StaticResource SecondaryText}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</Page>
